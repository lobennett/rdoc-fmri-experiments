# Analyzing Data

> This directory contains a script to convert raw JSON files to CSV files for each task.

## Instructions

> Note: Move the JSON data you want to convert to CSV to the ./raw directory before running the Python script.

```bash
chmod +x ./create_env.sh && ./create_env.sh && python3 main.py
```
